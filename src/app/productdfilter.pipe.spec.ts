import { ProductdfilterPipe } from './productdfilter.pipe';

describe('ProductdfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductdfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
